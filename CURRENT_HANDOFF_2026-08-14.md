# CJ96 Current Technical HandoffnnUpdated: 2026-08-18 13:22nn## 1. Current ObjectivennThe current work is reproducing the CJ96 board UI and behavior in the Tuya Ray panel, one board page at a time. The active page is Window2 (device list and device/valve-group editor).nnLatest reported issue:nn- In the APP Window2 editor, select capacity `2` in the one-click-add popup and press confirm.n- Expected: ungrouped solenoid valves are assigned to valve groups using the same algorithm as the board.n- Previous behavior: the APP popup closed, but the editor still displayed `[empty]` and the board did not execute auto-assignment.nn## 2. Important Pathsnn- Board/FlyThings project: `D:InstallFlyThingsIDEbinworkspacecj96`n- Active Tuya panel project: `C:UsersAdministratorTuYaMiniProjectminiapp`n- Panel mirror in board repository: `D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanel`n- Tuya bridge in board repository: `D:InstallFlyThingsIDEbinworkspacecj96integrationstuyabridge`n- Historical standalone bridge project: `D:codetuyacj96_tuya_demo`n- Panel output: `C:UsersAdministratorTuYaMiniProjectminiappdisttuya`n- Board GUI output: `D:InstallFlyThingsIDEbinworkspacecj96Releaselibzkgui.so`n- Embedded bridge runtime: `D:InstallFlyThingsIDEbinworkspacecj96runtimebincj96_tuya_demo`n- Generated embedded bridge header: `D:InstallFlyThingsIDEbinworkspacecj96srcgeneratedTuyaBridgeEmbedded.h`nn## 3. Mandatory Working Rulesnn- Use Python 3 for automation, file processing, builds, and board orchestration.n- Read Context Keeper recovery data before project actions after compaction/resume.n- Do not upload the panel or push GitHub unless the user explicitly requests it.n- The user runs Tuya real-device preview manually.n- The user currently prefers IDE quick-run for board delivery (`Ctrl+Alt+R` / project right-click quick run).n- Do not use `adb reboot`.n- Do not assume the current board binary, MQTT process, panel preview, network, or SD-card state from an older build.n- The worktree is dirty with many existing user changes. Do not revert unrelated files.nn## 4. Current Window2 Behaviornn### Device listnn- Six columns: address, name, type, valve-group number, status, operation.n- The list can scroll.n- Final row contains `Click to add` under address and `Sync` under operation.n- Custom devices from address 20 can show blue text `Delete` in operation.n- APP delete uses command F6 and waits for the full board table to return.nn### Add-device dialognn- Device address range is 20 through 255.n- Address 20 is the minimum; decrement must stop at 20.n- Sensor and solenoid-valve types are selectable.nn### Device editornn- Displays device address, device name, four valve-group rows, one-click add, cancel, and confirm.n- Right side displays selected valve-group number and association/clear/delete/rename operations.n- Selecting a row must update the selected valve-group display.n- The upper-left back button was removed from APP pages per user request.nn### One-click-add popupnn- Board-aligned dialog coordinates on the 1024x600 design canvas: `(236, 192, 550, 212)`.n- Capacity choices are 2, 3, and 4; default/selected state is green.n- Confirm must run the board's auto-assignment behavior, not only close the popup.nn## 5. Board Auto-Assignment AlgorithmnnThe board local path is in `src/logic/page2Logic.cc`:nn1. `closeIrrCapacityWindow(true)` commits the selected capacity.n2. `beginW2AutoAssignPreview()` stores a rollback snapshot.n3. `autoAssignUngroupedValves(capacity)` saves the current edited valve.n4. It collects every ungrouped solenoid valve.n5. Existing valve groups are filled first up to the selected capacity.n6. New groups are created using the smallest unused group number, up to group 128.n7. Device and valve-group lists are refreshed.nnRemote APP behavior now uses the same assignment strategy through `autoAssignPage2ValvesFromTuya(capacity)`.nn## 6. Latest One-Click-Add Fixnn### PanelnnFile: `integrations/tuya/panel/src/pages/home/index.tsx`nn- `confirmW2Capacity()` performs the same local assignment preview so the APP editor updates immediately.n- The editor list now calculates the actual valve addresses for each group and displays:n  - `Valve group[n]  Solenoid valve[addresses]`, orn  - `Valve group[n]  [empty]`.n- Confirm publishes `AA55F7NN55AA`, where `NN` is `02`, `03`, or `04`.n- The panel marks the operation as a pending auto-assignment and waits for the board's full table report.n- The board-returned table replaces APP local data and is the final source of truth.nnThe repository panel TSX and active Tuya project TSX are synchronized:nn- Size: 72571 bytesn- SHA-256: `d8c1721069fef2aa7ec1986e2ef853c19f8ba0b1ed5a37dc3cd5cd1d17eab261`nn### BridgennFile: `integrations/tuya/bridge/src/cj96_tuya_demo.c`nn- Parses `AA55F7NN55AA` for capacities 2 through 4.n- Writes `group_auto_assign=N` to the GUI home-command channel.nn### Board GUInnFiles:nn- `src/logic/mainLogic.cc`n- `src/logic/page2Logic.cc`nnBehavior:nn- `mainLogic.cc` recognizes `group_auto_assign=N`.n- Switches the board to Window2.n- Calls `autoAssignPage2ValvesFromTuya(N)`.n- Assigns all existing ungrouped solenoid valves.n- Refreshes Window2 data.n- Queues the complete device table back to the APP.nn# 7. Window2 Capacity Dialog Alignment (2026-08-16)nn- The active Tuya panel and repository mirror now use the board-sized Window2 capacity dialog.n- The title is 设置阀组容量; mode choices are 单个阀组 and 所有阀组.n- Capacity choices are 1, 2, and 3, using mode_select_off.png and mode_select_on.png at 42 x 42 design pixels.n- Single-group mode shows the current group number and capacity. The left and right arrows change those values, and the right-side capacity frames are not selected.n- All-groups mode temporarily shows - in the two left value boxes and highlights the selected right-side capacity frame.n- Switching modes does not destroy the cached per-group values. Cancel restores the complete dialog snapshot; confirm commits the preview and sends F7.n- Clicking the overlay outside the dialog is treated as cancel.n- The two left value boxes use explicit classes instead of nth-of-type, preventing layout drift when sibling views change.nn## 7. CJ96 DP101 Command FramesnnTuya DP:nn- DP code: `cj96_raw`n- DP ID: 101nnRelevant frames:nn- Screen sleep/wake: F0 family.n- Round irrigation on/off: F1 family.n- Stop schedule / advance group: F2 family.n- Rain delay: `AA55F3NN55AA`.n- Humidity threshold: `AA55F4NN55AA`.n- Device synchronization: `AA55F50155AA`.n- Delete custom device: `AA55F6AA55AA`, where `AA` is the address byte 20 through 255.n- Auto-assign valve groups: `AA55F7NN55AA`, where `NN` is 02 through 04.n- Network state reports use the E1 family.nn## 8. Device-Table Synchronizationnn- Protocol prefix: `CJDS1`.n- Supports a single-frame `T` report for small tables.n- Supports chunked `S`, `C`, `E`, and `R` reports with APP `A` acknowledgements.n- Maximum custom address range: 20 through 255 (236 custom devices).n- Default rows: addresses 1, 2, 6, 8, 9, and 10.n- Maximum tested total rows: 242.n- Chunk payload maximum: 900 hex characters.n- Frame maximum: 960 characters.n- Board Window2 discovery countdown: 14,860 ms, rounded up to 15 seconds (236 addresses, 60 ms per address, plus 200 ms start and 500 ms end guards).n- APP cloud/report watchdog: 30 seconds, intentionally separate from the board 15-second countdown; successful transfer ends early.n- Full-capacity protocol test result: 24 chunks, 27 frames, max frame 932, estimated 9.45 seconds.nnAPP-initiated operations that alter board data must trigger a complete table return. Board-local changes are currently not required to report proactively unless an APP operation requests/causes the change.nn## 9. Build Workflow and Verified Artifactsnn### Panel buildnnRun from the active panel project using Python to invoke Node/npm:nn```pythonnimport osnimport subprocessnnenv = os.environ.copy()nenv["PATH"] = r"D:InstallNodeJS;" + env.get("PATH", "")nsubprocess.run(n    [r"D:InstallNodeJSnpm.cmd", "run", "build"],n    cwd=r"C:UsersAdministratorTuYaMiniProjectminiapp",n    env=env,n    check=True,n)n```nnLatest result: Ray build passed on 2026-08-16.nn### Bridge preparationnn```pythonnimport subprocessnnsubprocess.run(n    ["python", "-X", "utf8", "tools/prepare_tuya_bridge.py"],n    cwd=r"D:InstallFlyThingsIDEbinworkspacecj96",n    check=True,n)n```nnThis compiles the T113 bridge, copies it to `runtime/bin`, and regenerates `TuyaBridgeEmbedded.h`.nn### Board GUI buildnn```pythonnimport subprocessnnsubprocess.run(n    ["python", "-X", "utf8", "tools/build_project.py"],n    cwd=r"D:InstallFlyThingsIDEbinworkspacecj96",n    check=True,n)n```nnLatest result: bridge compile, GUI compile, and `libzkgui.so` link all passed on 2026-08-14. Existing unused-code warnings remain.nnLatest artifact hashes:nn- `Release/libzkgui.so`n  - Size: 735012 bytesn  - SHA-256: `6d35990dc83d0f17575ec6bee851574ea9a91ebf748250a366e0c771aa38a7bc`n- `runtime/bin/cj96_tuya_demo`n  - Size: 259220 bytesn  - SHA-256: `7fa3b5b636faf99122784b1ee1df91bae08aceec4874438d7bc85e32e9757e65`nnBinary checks passed:nn- Bridge contains `AA55F7` and `group_auto_assign=`.n- GUI contains `group_auto_assign=` and the auto-assignment log string.n- `git diff --check` passed for the four main changed source files.nn## 10. Current Verification BoundarynnVerified now:nn- Active panel source matches repository mirror.n- Panel Ray build passes.n- T113 bridge builds and is embedded.n- Board GUI compiles and links.n- F7 command markers exist in final binaries.n- Full-capacity table framing test passes.nnStill requiring current-device verification:nn- The newly built board GUI/bridge has not been confirmed as the binary currently running on the physical board.n- The latest APP build has not yet been confirmed by a new real-device preview after this fix.n- The full cloud path `APP F7 -> Tuya -> bridge -> GUI -> table report -> APP refresh` still needs one physical test.nn## 11. Next Window: First Actionsnn1. Read this file and the root `AGENTS.md`/project instructions.n2. Do not repeat the completed implementation unless current files differ.n3. Ask the user to run a fresh Tuya real-device preview and IDE quick-run, or execute them only if explicitly requested.n4. Test with at least two ungrouped solenoid valves:n   - Open one valve editor.n   - Press one-click add.n   - Select capacity 2.n   - Confirm.n   - Check that the APP editor immediately shows valve addresses in group rows.n   - Check that the board Window2 shows the same assignments.n   - Check bridge log for `group_auto_assign=2`.n   - Check GUI log for `auto assign capacity=2`.n   - Confirm the APP receives the returned full device table.n5. If APP changes immediately but board does not, inspect F7 cloud/bridge logs before changing UI code.n6. If the board changes but APP reverts to empty, inspect the `CJDS1` returned group bitmap and panel `decodeDeviceTable()`.nn## 12. Source-Control StatennRelevant changed/untracked paths include:nn- Modified: `integrations/tuya/bridge/src/cj96_tuya_demo.c`n- Modified: `integrations/tuya/panel/src/pages/home/index.tsx`n- Modified: `runtime/bin/cj96_tuya_demo`n- Modified: `src/logic/mainLogic.cc`n- Modified: `src/logic/page2Logic.cc`n- Untracked/generated: `src/generated/TuyaBridgeEmbedded.h`nnThere are many additional pre-existing worktree changes. Preserve them. No GitHub push was performed for this work.nn## 13. FlyThingsIDE `Ctrl+Alt+R` 临时快速推送模式nn这是当前 CJ96 Window2/Window11 调试时使用的临时运行覆盖，不是永久固件烧录。nn### 操作与边界nn1. 使用 FlyThingsIDE `Ctrl+Alt+R`，或工程右键中的 quick run。n2. 该模式将当前构建产物推送到板端 `/mnt/extsd`，用于本次调试覆盖。n3. 当前 CJ96 需同步的文件为 `EasyUI.cfg`、`libzkgui.so`、`ui`、`resources`、`font`；以 IDE 传输窗口实际列出的文件为准。n4. 必须等待 IDE 传输窗口完整结束；传输中途不取消、不断电、不在文件尚未完成时判断 UI 逻辑。n5. 重新上电后，板端回到 `/res` 中的内置程序；`/mnt/extsd` 中的快速运行内容不当作永久固件保留。nn### 每次快速推送后的验证顺序nn1. 先确认工程构建成功。n2. 再确认 IDE 传输窗口已完整结束。n3. 板端进入界面后检查触摸是否响应，并查看 `initLib` 日志。n4. 重点排查 `initLib error`、`symbol not found` 和 Window11 启动后卡死。n5. 只有在构建、传输、板端实际运行三项分别通过后，才开始验证 Window2/Window11 的关联传感器、修改名称、移除和转移操作。nn### Window11 布局固定事项nn- 当前 Window11 外部点击区域使用 `LayoutPosition(236, 192, 550, 212)`。n- 不要重新引入板端运行库未提供的绝对位置 API；优先沿用已在一键添加弹窗验证过的弹窗处理方式。nnn## 14. 2026-08-16 APP ??????????nn- APP `confirmW2Capacity()` ????????????????????????n- ?????????????????????????n- ????????????????????????????????????? 1..128?n- ???????????????????????? 2??????????????n- APP ????????? `AA55F8GGCC55AA`??????? `AA55F9CC55AA`?? `F7` ??????n- Tuya bridge??? `mainLogic.cc` / `page2Logic.cc` ?????????????????????n- ????APP `npm run build`?bridge `build.py`??? `make -f Release/makefile` ??? 0?n- ????????????? Tuya ?????IDE `Ctrl+Alt+R` ??????????????nn## 15. 2026-08-17 APP 一键添加与同步边界nn本次 Window2 逻辑固定如下：nn- 只有“同步”按钮发送 `DEVICE_SYNC_FRAME`，触发面板重新扫描设备，并显示同步倒计时。n- APP “一键添加”不发送 `DEVICE_SYNC_FRAME`，不重新扫描设备，也不显示设备同步倒计时。n- APP “一键添加”只根据当前 APP 已有设备表生成本地预览，并发送阀组容量配置命令 `F8/F9`。n- 面板完成容量分配后，通过现有配置结果表回传机制返回最终设备表；APP 在收到完整且校验通过的结果表后刷新显示。n- 一键添加时，只对未分组的电磁阀按地址升序分配；已存在阀组的设备保持原阀组，不因再次一键添加而重排。n- 普通同步发现的新设备先显示为未分组，不自动塞入阀组1。n- 一键添加等待结果表期间使用独立的 `deviceReportPending` 状态；超时提示为“一键添加超时，请重新同步”，与设备同步倒计时状态分离。nn已验证：nn- APP 主工程与仓库镜像 `index.tsx` 内容一致，SHA256 为 `ed4f61b8cfeb9ff5c3237bdb826c78b15c24e36827fee9df9a69751b86fbe790`。n- `C:UsersAdministratorTuYaMiniProjectminiapp` 执行 `npm.cmd run build` 返回码为 `0`，Ray 构建完成。n- 尚未执行本轮真实 Tuya 预览、IDE `Ctrl+Alt+R` 快速推送和实体板验证。nnn## 16. 2026-08-17 APP与本地面板单一数据源方案nn当前采用以本地面板为唯一业务数据源：nn```textnAPP 发送命令n    -> 本地面板读取、修改、保存 DeviceDataStoren    -> 本地面板回传完整设备表n    -> APP 只负责显示n```nn- `AA55F50255AA`：仅读取面板当前设备表，不扫描。n- `AA55F50155AA`：只有“同步”按钮触发重新扫描，面板倒计时以板端逻辑为准，APP另用 30 秒作云端回传监听超时。n- “一键添加”只发送 `F8/F9` 容量命令，不再重新扫描，由面板按当前表完成分组后回传最终表。n- 本轮已完成读表、同步、一键添加的面板主数据源改造；关联传感器、清空、改名、转移等编辑操作后续需分别接入面板命令后才能完全改为同一模式，不将 APP 本地预览当作最终数据。nn### 流量估算nn单条设备记录在当前协议中最大约 60 字节；236 个独立地址的二进制表约 14,160 字节，转十六进制后约 28,320 个 ASCII 字符，加上分片协议头和 MQTT/TLS 开销，一次完整表同步仍是“几十 KB 级”，不会达到 MB 级。按 900 个 hex 字符分片时，大约 32 个数据分片加开始、结束和结果帧。nn一个读表请求本身只有 12 个 ASCII 字符左右；高流量操作是反复点开 Window2 并反复请求完整表。版本/hash 查询与缓存已在第17节实现：版本一致时仅回复短确认帧，只有变更时才回传整表。nn### 本次构建证据nn- Tuya bridge 构建返回 0，产物约 260,256 字节。n- 板端 GUI 执行 `python -X utf8 tools/build_project.py` 返回 0，生成 `Release/libzkgui.so`。n- APP 执行 `npm.cmd run build` 返回 0。n- 尚未执行本轮实机预览、IDE `Ctrl+Alt+R` 快速推送和实体板验证。nn## 17. 2026-08-17 APP设备表版本/hash缓存与流量压缩nn本轮已完成“版本一致直接显示 APP 旧数据”的实现，缓存按设备 ID 隔离：nn- APP第一次没有本地缓存时发送 AA55F50255AA，读取面板当前设备表。n- APP收到完整表并通过 FNV-1a hash 校验后，同时保存设备表、设备表 hash 和保存时间到本地同步存储；缓存 key 为 cj96:device-table:<deviceId>。n- APP再次打开 Window2 时，如果存在有效缓存，发送 AA55F503<8位HEX_HASH>55AA，不再发送完整读表请求。n- 面板返回 CJDS1|V|<session>|S|<count>|<hash> 且 hash、记录数量与缓存一致时，APP直接保留并显示缓存表，结束等待状态，不接收设备表分片。n- 面板返回 CJDS1|V|<session>|C|<count>|<hash> 时，APP继续接收原有 S/C/E/R 完整表分片，校验通过后更新界面、hash 和本地缓存。n- 缓存结构带 schema、deviceId、hash、设备记录和保存时间；缓存损坏、设备 ID 不匹配、hash 不合法或记录结构不完整时自动放弃缓存，回到完整读表路径。n- APP本地编辑阀组、关联、清空或删除阀组时先清理旧缓存 hash，避免面板数据尚未回传时误判为版本一致；最终以面板回传的完整表为准。nn### 流量和速度效果nn- 236个独立地址的一次完整表仍使用原有可靠分片和整表 hash 校验，正确性路径不变。n- 版本一致时，网络上只发送一个短 hash 查询和一个短版本响应；设备表本身不再重复传输，重复进入 Window2 的设备表流量从约几十 KB 降到几十字节级，等待时间也从完整分片等待缩短为一次短响应。n- 版本发生变化时仍然完整读取新表，不用旧缓存冒充新数据；因此压缩只发生在确认一致的场景。n- 这里使用 hash 作为版本指纹，不依赖本地递增版本号；面板任何设备地址、类型、名称、连接状态或阀组位图变化都会导致 hash 变化。nn### 本轮构建证据nn- C:UsersAdministratorTuYaMiniProjectminiapp 执行 npm.cmd run build 返回码 0，Ray 构建完成。n- APP主工程与仓库镜像的 Window2 TSX 已同步。n- python -X utf8 tools/test_tuya_device_sync_protocol.py 回归测试通过：rows=242、custom=236、chunks=24、hash=87FA17F4。n- python -X utf8 tools/build_project.py 返回码 0，板端 GUI 与 Tuya bridge 构建完成。n- 尚未执行 Tuya 真机预览、IDE Ctrl+Alt+R 快速推送和实体板验证。nn## 18. 2026-08-17 Window2 refresh latency optimizationnnObserved path before this change: APP showed the cloud publish callback as send succeeded while the actual board table was still pending. The board consumed the Window2 Tuya command only from the 1000 ms UI timer, and the bridge used a 100 ms main-loop sleep plus a 350 ms device-report frame interval.nnChanges:n- APP requestCurrentDeviceTable no longer shows a misleading success toast for the version/read query. The cached Window2 table remains visible immediately; the query runs silently in the background.n- Board handleTuyaDeviceSyncCommand now runs on the existing 100 ms timer instead of the 1000 ms timer. Normal home commands remain on the 1000 ms timer.n- Tuya bridge device-report interval changed from 350 ms to 80 ms. Bridge loop sleep changed from 100 ms to 50 ms. Protocol frames, hash validation, ACK handling, and the full-sync path are unchanged.nnBuild verification: APP npm.cmd run build returned 0; board python -X utf8 tools/build_project.py returned 0. Runtime delivery and real APP/board timing still require the user IDE Ctrl+Alt+R quick-run and a real Window2 test.nnn## 19. 2026-08-17 Window2 group-capacity syncnnImplemented the confirmed design: the board remains the single source of truth, and entering APP Window2 performs a cached display plus a background version/hash check. The board now returns the complete valve-group maximum-capacity table together with a changed device table.nn- Added protocol frame CJDS1|G|<SESSION>|<256-hex-capacity-bytes>|<8-hex-joint-hash> before T or S/C/E/R full-table frames. The 128 bytes map to valve groups 1..128; each value is clamped to 1..3, including empty groups.n- The version hash is now FNV-1a over device-table-bytes + 128 capacity bytes, so changing only a group capacity invalidates the APP cache.n- APP cache schema is now 2 and stores groupCapacities for all 128 groups. Schema-1 caches are discarded and refreshed from the board.n- APP validates the G frame, keeps the capacity map during both single-frame and chunked table transfers, updates the Window2 capacity state, and writes devices plus capacities together to cache.n- Active APP project and repository panel mirror are byte-identical.n- Protocol regression passed: nows=242, custom=236, chunks=24, nrames=28, max_frame=932, joint hash 35F7AEF4.n- APP build passed with npm.cmd run build; board GUI and Tuya bridge build passed with python -X utf8 tools/build_project.py.n- Built artifacts: Release/libzkgui.so SHA-256 5b68192168e2b424784f3f04b2b3aea4f003cd913d663e615e6f42d0f18df04d; nuntime/bin/cj96_tuya_demo SHA-256 31f3ea783b6a299c1365c26773c7e5baf2b1bed8cf06df51d129a113b7e47a1a.n- Not uploaded, not pushed, and not flashed. Real-device verification remains: user runs IDE Ctrl+Alt+R, opens APP Window2, checks that empty-group capacities and capacity-only changes are reflected without a new address scan.nnnnn## 2026-08-17 Window2 group highlight and system settings icon repairn- Local board Window2 valve-group list now uses a selected-row surface matching the APP: normal #F6FBFF, selected/pressed #DCEEEF. The ChangeIrr_ListView selected state is retained through the reusable tools/style_change_irr_like_device_list.py helper.n- The missing local system-settings time icon was traced to a corrupted resources/topset_time_113.png (all FF bytes). It was restored from the valid ui/topset_time_113.png. The other six settings icons are present and valid: WiFi, Ethernet, 4G, language, display, and debug.n- ui/main.ftu and Release/remote_main.ftu are synchronized. Backup: Release/ui_redesign_backup/w2_group_selection_settings_20260817_151500.n- GUI/bridge build command python -X utf8 tools/build_project.py returned 0 and produced Release/libzkgui.so. Real-device quick-run and visual verification remain pending until the user runs the preview.nn## 20. 2026-08-17 UI freeze diagnosis and guardnnLatest board symptom: after quick-run, the whole UI can stop responding: touch input and the top-right clock stop together. This indicates UI event-loop re-entry/blocking rather than an isolated clock widget fault.nnApplied source guard in `src/logic/page2Logic.cc`: `refreshChangeIrrListView()` is now single-pass, and programmatic `setSelection()` is marked so the selection callback cannot reopen Window11, bind devices, or refresh the same ListView recursively. This targets the path `refreshListView -> setSelection -> onChangeIrrListItemClick -> selectIrrGroup -> refreshChangeIrrListView`.nnBuild verification on 2026-08-17: `python -X utf8 tools/build_project.py` returned 0. Artifact: `Release/libzkgui.so`, SHA-256 `3ae37905a6679479a6c5ff969a7cb8cc4d12cba4efaba531307def281c15b477`. Board quick-run validation is still pending.nn## 21. 2026-08-17 Window2 selected colornnWindow2 ????????????? `#005BBB`????? ARGB `0xFF005BBB`??????? `#F6FBFF`?nn2026-08-17 ???? 0??? `Release/libzkgui.so` SHA-256?`131fd50cc7bd30ee77a0501c023dee1ed0f1b8746b8d972c02bbe0ba62cad6ec`?nn## 22. 2026-08-17 Window2 selected border bluenn??????????????Window2 ????????????????? `#0E97E8`??? ARGB `0xFF0E97E8`??????? `#F6FBFF`?????? `tools/add_irr_capacity_dialog.py` ??????????nn???? 0?`Release/libzkgui.so` SHA-256?`3b262739265f7e572495e4db91dab3918c06080a7f88cc273f97022d5b95a01f`?nn## 23. 2026-08-17 Window11 remove and transfer icon colorsnnWindow11 ????????????? 120x60 ?????????????????? `#005BBB`?????????????`resources` ? `ui` ???????????nn???? 0?`Release/libzkgui.so` SHA-256?`3b262739265f7e572495e4db91dab3918c06080a7f88cc273f97022d5b95a01f`?nn## 2026-08-17 Persistent Display-Power Diagnostic Log (corrected)nn- **主持久分区路径（以此为准）：**n  `/data/cj96_logs/display_power.log`n- **日志轮换文件：**n  `/data/cj96_logs/display_power.log.1`n- `/data` 是板端当前使用的内部持久分区，显示电源诊断日志的主要保存位置。n- `/mnt/extsd/cj96_logs/display_power.log` 和 `.1` 只是外部存储镜像，外部存储存在并且可写时同步生成；它不是本次验证的持久主路径。n- 每个目标独立执行 `mkdir`、追加写入、`fflush`、`fsync`、`fclose`。外部存储不可用时，`/data` 日志继续写入。n- 单个日志达到 256 KB 后轮换到 `.1`。n- 日志内容包括初始化、每 10 秒一次 heartbeat、`sleepScreen`/`wakeScreen` 开始与完成耗时，以及触摸唤醒请求。n- 实现文件：n  `D:InstallFlyThingsIDEbinworkspacecj96srclogicDisplayPowerManager.cc`nn### 当前板端验证记录nn- 用户通过 IDE `Ctrl+Alt+R` 推送后重新上电，板端出现：n  `/data/cj96_logs/display_power.log`n- 读取到的文件大小为 595 bytes，内容包含初始化记录和 `heartbeat tick=10` 至 `heartbeat tick=50`。n- `/mnt/extsd/cj96_logs/display_power.log` 同步存在，大小和内容与 `/data` 当前记录一致。n- 本次已确认日志正在写入 `/data`。下一次完整断电上电后，继续检查同一路径中的旧 heartbeat 是否仍保留，用于完成跨上电留存验证。nn## 2026-08-18 Window2 entry device-table query wirednn- APP进入 Window2 时现在会触发 `requestCurrentDeviceTable()`。n- 如果缓存存在，先立即显示缓存，同时发送 `AA55F503 + 缓存 hash` 给本地面板。n- 面板 hash 一致时保留缓存；hash 变化时接收完整设备表并覆盖 APP 缓存，因此本地已经删除的地址（例如 32、33）会从 APP 表中消失。n- 查询入口带设备 ID 和同步状态保护：同一次 Window2 停留只查询一次；离开 Window2 后重新进入会再次查询；正在手动同步时等待同步完成。n- 活动工程和仓库镜像已同步：`C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.tsx`、`D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanelsrcpageshomeindex.tsx`。n- 修改备份：`D:InstallFlyThingsIDEbinworkspacecj96backupswindow2_query_on_enter_20260818_091627`。n- APP build verified 2026-08-18: npm.cmd run build returned 0 and generated C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js; live APP preview still requires republish; no board burn or adb reboot performed.nnn## 2026-08-18 APP native status-bar reapplynn- The APP custom header already renders its own clock, date, weekday, and network icon.n- hideStatusBar() was previously called only once when the Home component mounted, so the phone native status bar could return after app resume or internal page changes.n- Added a stable keepCustomHeaderFullscreen callback, re-applied on APP onShow, initial mount, and Window2/other internal page changes.n- Updated both active APP source and the repository mirror; SHA-256 is identical.n- Backup: D:InstallFlyThingsIDEbinworkspacecj96backupsstatus_bar_reapply_before_20260818_102554.n- APP build passed on 2026-08-18; live preview still requires republish.nnn## 2026-08-18 Window2 soft white sync transitionnn- APP-only change. Board firmware was not modified, burned, rebooted, or deployed.n- Window2 bottom navigation now calls openWindow2() instead of opening the page immediately.n- When a device ID is available, the APP first shows a full-canvas soft white overlay with a low-contrast blue pulse and 同步中，请稍候; Window2 is opened only after the table query finishes.n- Cached-hash response V|S completes the overlay quickly and keeps the APP cache. Changed-hash response V|C stays on the overlay while the full table transfer completes, then replaces the APP device table and opens Window2.n- Direct full-table T, chunk-transfer R, checksum/receive errors, publish failure, and timeout all close the overlay so the APP cannot remain stuck on white.n- Removed the old ctivePage === device effect query to avoid sending a second query after the explicit Window2 open flow.n- Changed files in the active APP project:n  - C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.tsxn  - C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.module.lessn- The same two files were copied to the panel repository mirror:n  - D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanelsrcpageshomeindex.tsxn  - D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanelsrcpageshomeindex.module.lessn- APP build verified on 2026-08-18 with npm.cmd run build return code 0. Generated artifact: `C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js` (491828 bytes, SHA-256 `14ef752db51c42ce11f28725d91f1bce3c8e11fd2ebb150d06118b4082612bea`).npm.cmd run build return code 0. Generated artifact: C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js (491828 bytes, SHA-256 14ef752db51c42ce11f28725d91f1bce3c8e11fd2ebb150d06118b4082612bea).n- The active APP source and mirror SHA-256 values match. Real-device behavior still requires republishing the APP for preview; no board re-flash is part of this change.nnn## 2026-08-18 Window2 5-second entry delay fixnn- Root cause: `openWindow2()` previously kept Window2 hidden until the board/cloud device-table response completed; this made the communication round trip appear as a 5-second page freeze.n- No fixed 5-second wait exists in the APP. Version query, board command polling, and bridge loop are asynchronous.n- APP now enters Window2 immediately and keeps the cached table visible while the current table query runs in the background.n- The soft white transition is visual only and is capped at 320 ms; the later `V|S`, `V|C`, `T`, or `R` response updates the table without blocking page entry.n- `completeWindow2Opening()` no longer switches pages after the response, preventing a delayed response from forcing the user back into Window2 after navigating elsewhere.n- Changed files: `C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.tsx` and the repository mirror under `D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanelsrcpageshomeindex.tsx`.n- Backup: `D:InstallFlyThingsIDEbinworkspacecj96backupswindow2_no_blocking_5s_before_20260818_124929`.n- APP build passed on 2026-08-18; artifact `C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js`, 487569 bytes, SHA-256 `a129c9947e2a2d6d3c4c098630744454afc1fb256f0cfea6b974524dd2510fe4`.n- Board firmware was not changed or burned. APP republish and real-device preview are required for runtime verification.n

## 2026-08-18 Window2 soft transition and rapid re-entry fix

- APP-only change; board firmware was not modified, burned, rebooted, or deployed.
- Root cause of Window2 re-entry no response: openWindow2() returned when sending, syncingDevices, or deviceReportPending was true. A previous background table read could leave deviceReportPending true while the user had already left Window2.
- openWindow2() now changes to Window2 immediately even when the previous table request is still pending. 
equestCurrentDeviceTable() remains the duplicate-request gate, so returning to Window2 does not start a second read.
- The transition overlay now uses a short fade-in and a 190 ms fade-out before unmounting, rather than disappearing in one frame. It is visual-only (pointer-events: none) so it cannot block a rapid tap on the bottom navigation.
- Re-entry clears stale transition timers and resets the closing state. Timer cleanup was added for component teardown.
- Changed active APP files: C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.tsx and index.module.less. The same files are synchronized in integrationstuyapanelsrcpageshome.
- Backup: D:InstallFlyThingsIDEbinworkspacecj96backupswindow2_soft_transition_reentry_before_20260818_130908.
- APP build passed on 2026-08-18 with 
pm.cmd run build; artifact: C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js, 492371 bytes, SHA-256 10a4200795439b7b3019abd3b08a971227fcd9d989283719ff9b06c7471c2084.
- Active APP source and repository mirror SHA-256 values match: TSX 50b0000695164a3e68d3822553dc010b99fe7fa5ce1a037adc881b86a4a2942; LESS 9e58a0afe0fc22dea71082f1de662b713a5b0cf9f33d669877d745b9f7efd97.
- Real-device behavior still requires republishing the APP and testing Window1 → Window2 → Window1 → Window2 quickly.


## 2026-08-18 Window2 stale APP cache refresh fallback

- User symptom: local board device data changed, but APP Window2 flashed briefly and still showed the old cached table.
- APP-only fix; board firmware was not changed, burned, rebooted, or deployed.
- When Window2 opens with a cached hash, APP sends `AA55F503 + cached hash + 55AA` and keeps showing the cache immediately.
- If the version response does not arrive within 1800 ms, APP automatically falls back to the complete table read `AA55F50255AA` in the background.
- If the board reports `V|S` but the APP cache is missing, inconsistent, or has a different count/hash, APP also falls back to the complete table instead of keeping stale rows or stopping with a false cache error.
- When an earlier query has expired, Window2 clears the stale pending flags and is allowed to issue a new query; this removes the condition where `deviceReportPending` could permanently suppress refresh.
- Version fallback timers are cleared when a `CJDS1` report, publish failure, timeout, or component teardown is handled.
- Changed active APP file: `C:UsersAdministratorTuYaMiniProjectminiappsrcpageshomeindex.tsx`.
- Synchronized repository mirror: `D:InstallFlyThingsIDEbinworkspacecj96integrationstuyapanelsrcpageshomeindex.tsx`.
- Active source and mirror SHA-256: `19fb17185f3d11a6ae9cc9719b5a9713176fb2f9d81534b529d551a08a9da47f`.
- APP build passed on 2026-08-18 with `D:InstallNodeJSnpm.cmd run build`; artifact: `C:UsersAdministratorTuYaMiniProjectminiappdisttuyapageshomeindex.js`, 499034 bytes, SHA-256 `8a964fe3f7c548a7af5578166a3e79aae7d9cd7390bcca5095e4a0279f32048e`.
- Real-device behavior still requires republishing the APP. No board re-flash is part of this change.

## Window2 ???????????2026-08-18?

???????????????APP ??????????????APP ???????????????????????????????????????????

### Window2 ????

1. APP ?? Window2 ??????????????????????? 5 ??
2. ???????APP ?????????`AA55F503 + cacheHash(8?????) + 55AA`??????? `device_sync/version=XXXXXXXX`?
3. ????????????????????????????
4. ??????????????????????? 1800 ms ?????APP ?????????`AA55F50255AA`??????? `device_sync/read`?
5. ?????????APP ????????????????????????????????????????
6. Window1 ? Window2 ? Window1 ? Window2 ????????????????????????????????????????

### ??????

- ????????????????? `device_sync/sync`?????????????????????
- ?? Window2 ????????????????????????????????
- ??????????????????????????????????????APP ????????
- ??? APP ?????????????? Window2 ???????????????????????

### ???????

APP ??????? inbox ??????????????

`CJ96CMD1|command_id|seq|kind|command`

???????????????

`CJACK1|command_id|seq|succeeded|error_code`

?????????`fflush`?`fsync` ? `rename`??????????? ACK ???????? inbox??????????? `/mnt/extsd/cj96_tuya_demo`??? `/data/cj96_tuya_demo`?????? `/tmp/cj96_tuya_demo`?

### ??????

- APP ?????????? `index.tsx` SHA-256 ???`51fdda8dc9de80d7ea058924ec7024c02bc67cacb1cf918981b175d545c60a23`?
- APP ?????????? `index.module.less` SHA-256 ???`f9e58a0afe0fc22dea71082f1de662b713a5b0cf9f33d669877d745b9f7efd97`?
- APP ?????2026-08-18?????`C:\Users\Administrator\TuYaMiniProject\miniapp\dist\tuya\pages\home\index.js`?SHA-256?`5cf3a64a4d100a976105005675b6e160d99f10c8658b2755de639dc49b2a7660`?
- Bridge ?????????? `runtime/bin/cj96_tuya_demo` ? Bridge ???? SHA-256 ???`b9170c2ea20d3a8be9f485d195c44fd94969c621a3dd812ebff1e894fd91c0d3`?
- ???????????`src/generated/TuyaBridgeEmbedded.h`?SHA-256?`2047b5e1e2431b57868a16e48ef3774f9b8615f31575246cdd6f617f256f5cc3`?
- FlyThings Release ????????`Release/libzkgui.so`?SHA-256?`ff013aa06b58f90175265d5becd3c9b158f605d0259761b01a042ea36e50fa7b`?
- ?????? APP ????????????????????????????? APP??? FlyThingsIDE ??? `Ctrl+Alt+R` ???????

## Window2 APP?????????????2026-08-18?

### ??

APP ? Window2 ???????/?????????????????? APP ? React ??????????????????????????????????????????????????????APP ????????

### ????

1. APP ?????????????????
2. APP ????????`AA55FA + groupCode + count + address1...addressN + 55AA`?
3. `groupCode` ? `01..80` ???????????`FF` ?????????
4. Tuya Bridge ????????? `home` ???`group_bind=groupCode,address1,address2,...`?
5. ?? `mainLogic.cc` ??????? `bindPage2DevicesFromTuya()`?
6. ?????????????????????????? Window2 ????????????????
7. APP ???????????????????????????????

### ????

Bridge ??????? 192 ??????????????? 60 ????APP?Bridge ????????? 60 ????????????????????

### ??????

- APP ????`C:\Users\Administrator\TuYaMiniProject\miniapp\src\pages\home\index.tsx`
- APP ?????`D:\Install\FlyThingsIDE\bin\workspace\cj96\integrations\tuya\panel\src\pages\home\index.tsx`
- Bridge?`D:\Install\FlyThingsIDE\bin\workspace\cj96\integrations\tuya\bridge\src\cj96_tuya_demo.c`
- ??????`D:\Install\FlyThingsIDE\bin\workspace\cj96\src\logic\mainLogic.cc`
- ?? Window2 ???`D:\Install\FlyThingsIDE\bin\workspace\cj96\src\logic\page2Logic.cc`

### ??????

- APP `D:\Install\NodeJS\npm.cmd run build`????
- Bridge `python integrations/tuya/bridge/build.py`????
- Bridge ?? `python tools/prepare_tuya_bridge.py`????
- FlyThings Release `python tools/build_project.py`????
- Window2 ????????????`rows=242 custom=236 table_bytes=10406 chunks=24 frames=28 max_frame=932 estimated_seconds=2.24`?
- APP ????? `index.tsx` SHA-256?`03b90d784a2e1430f16c9b40b1e22a604513f3074a563e89c7f7262ed9ddee66`?
- APP ???? `C:\Users\Administrator\TuYaMiniProject\miniapp\dist\tuya\pages\home\index.js` SHA-256?`5651200711a47fce18373822b202fda16c182ae8eeb7ffef1881ac78da46f330`?
- Bridge ???? `D:\Install\FlyThingsIDE\bin\workspace\cj96\runtime\bin\cj96_tuya_demo` SHA-256?`4a7ce6e8bbbdd468b6eca999fe9c6a587e34589b632e41710519b906254993e7`?
- ????? `D:\Install\FlyThingsIDE\bin\workspace\cj96\src\generated\TuyaBridgeEmbedded.h` SHA-256?`c179571ddee2026020b7e94f96fdadcb21d18df9de97a48240d32095215ec865`?
- FlyThings Release `D:\Install\FlyThingsIDE\bin\workspace\cj96\Release\libzkgui.so` SHA-256?`abf3e0f9e682840a208a4e32d854ccbe60107c45a4247629f2aa7f6513ab6cc2`?

### ??????

????????????????????????APP ????? `Ctrl+Alt+R` ?????????????????????? APP ???? 32 ????? 1??? 33 ????? 3????????? Window2 ?????? 32??? 1??? 33??? 3?????? Window2 ??????????????
## 2026-08-19 External Display-Power Log Routing

- `src/logic/DisplayPowerManager.cc` now writes display-power diagnostics only to `/mnt/extsd/cj96_logs/display_power.log` and its `.1` rotation file.
- The internal `/data/cj96_logs/display_power.log` target was removed so display diagnostics do not consume the 384 KiB `/data` partition.
- `/data/cj96_tuya_demo.conf` remains the active Tuya bridge configuration path. The current board file size is 230 bytes.
- Source build passed with `python tools/build_project.py`; `Release/libzkgui.so` was regenerated and deployed to `/mnt/extsd/lib/libzkgui.so` on board `192.168.1.70:5555` using the focused library deployment script. The old `/data/cj96_logs/display_power.log` was removed; `/data/cj96_logs` remained empty for 20 seconds while `zkswe` returned to `running`.

