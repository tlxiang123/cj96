/*
 * Rs485Bus is implemented inline in Rs485Bus.h.
 * Keep this file as an empty translation unit so generated makefiles can
 * compile it without producing duplicate symbols.
 */
#include "uart/Rs485Bus.h"
