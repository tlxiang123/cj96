#pragma once

// Installed firmware version shared by the GUI and the embedded Tuya bridge.
// Bump this before building each new OTA release; do not use the download target.
#define CJ96_FIRMWARE_VERSION "1.0.68"
